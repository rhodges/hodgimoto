'use strict';

/**
 * @ngdoc function
 * @name hodgimotoApp.controller:WorksCtrl
 * @description
 * # WorksCtrl
 * Controller of the hodgimotoApp
 */
angular.module('hodgimotoApp')
  .controller('WorksCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
